<%= form_with(model: [@question, @answer ], local: true) do |form| %>
  
  <p>
    <%= form.label :body %>
    <%= form.text_area :body %>
  </p>

  <p>
    <%= form.label :correct %>
    <%= form.check_box :correct %>
  </p>


  <div class="actions">
    <%= form.submit %>
  </div>

<% end %>
